name: Verify Sera M01 in Chromium

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused-during-completion-pass:
    runs-on: ubuntu-latest
    steps:
      - run: echo "M01 verification is temporarily manual while the canonical vertical slice is committed."
