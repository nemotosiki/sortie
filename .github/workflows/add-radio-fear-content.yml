name: Add radio fear escalation content

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  dormant:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow is staged and will be activated after the patch script is present."
