name: Add named ace Ironback

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  dormant:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow staged; the patch script will activate it in the next commit."
