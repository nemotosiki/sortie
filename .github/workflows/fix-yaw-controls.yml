name: Fix yaw and throttle controls

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: write

jobs:
  registration-probe:
    runs-on: ubuntu-latest
    steps:
      - run: echo "yaw workflow registered"
