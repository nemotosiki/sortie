name: Restyle tactical radar targets

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  dormant:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow staged; the next commit activates the one-time static publish."
