# SPEC ウェーブ編成の多様化 (2026-07-26, Fable設計)

ユーザー指摘: **「必ず敵戦闘機は3体しか同時登場しないという縛りはないので、いろんな
バリエーションでミッションを構築して。毎回3体だけじゃん」**

## 現状の問題

1ウェーブ3機固定のハードコードは既に撤廃済み（`{types:[...]}` の自由編成が使える）。
にもかかわらず**全ミッションが `air1`/`air2` プリセット（どちらも3機）を使い続けている**ため、
プレイ体験としては毎回3機のまま。**編成そのものを組み直す**。

## 設計方針

### 1. 機数のレンジを広げる
- 序盤: **2〜3機**（弱い機体で密度も低い）
- 中盤: **4〜6機**
- 後半: **6〜9機**（エース込み、同時展開あり）
- 「機数＝難度」ではない。**MiG-21が6機 < Su-35Sが3機** になるよう、HP合計と旋回性能で釣り合わせる

### 2. 質の混成（同種3機を並べない）
- 1ウェーブに**役割の違う機種を混ぜる**: 例「MiG-29×2＋Su-25×1」（速い/硬い）、
  「MiG-21×4＋MiG-23×1」（雑魚の群れ＋中堅）、「Tu-95×2＋MiG-29×2」（爆撃機＋護衛）
- 空と地上/海上の**同時進行**（`concurrent: true`）を活用し、「空を捌きながら艦を沈める」等の
  同時処理を作る

### 3. 到着タイミングの変化
- 一度に全部出さない。**band**（ウェーブ帯）を使って**時間差で増援**を入れる
- 「削り切る前に次が来る」圧、「1機だけ先行して来る」偵察、など緩急をつける
- 逆に**間**を作るミッションもあってよい（全滅させてから次が来る = 息継ぎ）

### 4. TGT制度と組み合わせる
- 非TGTの増援を混ぜて「無視して先へ進む」判断を作る
- TGTだけ少数（例: 爆撃機2機）で、周囲に非TGTが多数、という構図はACらしい

## 各ミッションの編成指針（実装者が具体値を決めてよい）

| ミッション | 現状 | 変更方針 |
|---|---|---|
| m01 FIRST CONTACT | 爆撃機2＋護衛2 | 維持（チュートリアル。ただし護衛はMiG-21×2のまま少数で） |
| m02 TWO-PRONGED | 3＋3 | **4＋5**（MiG-21主体、2波目にMiG-23を1機混ぜる） |
| m03 FLEET DESTRUCTION | 艦5＋CAP2 | CAPを**時間差で2波（2機→3機）**に。艦と同時進行 |
| m-glacier GLACIER RUN | 地上4＋空3 | 空を**峡谷内2機＋上空4機**に分け、到着を時間差に |
| m-night NIGHT RAID | 地上11＋空3 | 空を**5機**に増やし、うち2機を非TGTの増援として遅れて投入 |
| m-convoy IRON COLUMN | 車列7＋CAP2 | CAPを**4機**に、うち2機は非TGT。車列進行と同時 |
| m-swarm UAV SWARM | UAV3＋3 | **UAV 4＋4＋母機**。同時最大を上げる（軽量なのでfps余裕あり） |
| m-intercept BOMBER STREAM | 3波×3 | **爆撃機3＋護衛2 → Tu-95×2＋MiG-23 → 増援MiG-29×4**（山場を作る） |
| m04 IRONBACK | エース＋僚機2 | **エース＋僚機2、撃破後に増援3**（決闘に邪魔が入る演出） |
| m05 FINAL SORTIE | 3波 | **最終波を6〜9機**の総力戦に。エース3人（IRONBACK/LONGBOW/VULTURE）を別波に配置 |

## 制約

- **fps**: 同時8機以上のウェーブは必ずfps実測（≥50）。割るなら機数を落とす
- **パータイム**: 機数を増やしたミッションはパータイムを比例調整（クリア可能性を実走で確認）
- 難度曲線（序盤=弱い/後半=強い＋エース多め）を壊さない
- 敵は**ロシア機のみ**（陣営分離済み）
- 既存のミッションキー・解禁連鎖・レコード互換は変えない

## 検証（最小限）

1. 各ミッションのウェーブ構成をプローブで出力し、**3機固定になっていない**ことを確認（機数の分布表）
2. 全ミッションが起動し、少なくとも m01/m03/m05/m-intercept はクリア可能
3. 最大同時数のウェーブでfps実測（≥50）
4. エラー0
