# 夜間バグ修正 報告書（2026-08-02晩 → 08-03朝）

**指示**: 「コード全体を読んで新たなバグを発見して直してほしい」「基本的にエースコンバットっぽい仕様に」
「ダサい"デストロイ"英語表記も消しておいて」「寝ている間も自分で課題を見つけて無限に動く」。

## 1. 何をやったか

41,000行の`index.html`＋`src/`＋`tools/`を、**2ラウンドに分けてOpus 5で並列精読**した。
第1ラウンドは14分割の範囲担当、第2ラウンドは範囲ではなく**8つの横断的な問い**
（状態遷移の全ペア／境界値とNaN／モジュール跨ぎの契約／dtとrawDtの振り分け／生成と解放の対応／
敵AIの縮退／セーブ互換／HUDと内部値のズレ）。出た所見はFable（メインループ）が
**全件実コードで裏取り**してから修正。1修正=1検証=1コミット。
**13バッチ・約70件を出荷**（うち致命/準致命8件）。全40ミッションの起動検証を各段階で通した。

## 2. 効きの大きい修正（プレイに直接効く順）

| # | 症状 | 真因 | 証拠 |
|---|---|---|---|
| 1 | **露キャンペーンが1本も出撃できない** | キャンペーン切替時の機体初期化がロスター先頭(未所持Su-37)に落ち、LAUNCHが購入確認フローに吸われる。米編は先頭がスターターF-16で偶然隠れていた | 修正後 r05が新規プロファイルでMiG-21起動。**露編20本すべて起動を実測** |
| 2 | **戦域離脱の帰投矢印が南北逆** | `atan2`のZ符号がファイル規約と逆。警告中のプレイヤーを常に逆方向へ誘導していた | 規約(`北=-Z`)と消費側の両照合 |
| 3 | **チェックポイント復帰で護衛が誤全滅** | 護衛機を全機生存で復活させながら喪失数だけ巻き戻すため、残数が盤面と乖離。1機落ちた瞬間に「CONVOY DESTROYED」で即失敗 | 残数を盤面基準へ。同根の二重計上・整合性免責・マーク消失・撃破済み地上目標の復活も同時修正 |
| 4 | **夜間基地の11目標が7.4m地中に埋没** | 設置物が高さ0.92クランプ付きサンプラーを使っていた（車両だけ無クランプ版を使っていた） | m-night全11基が y=84.64 → **92.00（天面ぴったり）** |
| 5 | **斉射の後続弾が火球を素通り** | 撃墜済み目標を誘導対象から外していたため信管が働かず、無得点で燃料切れまで飛ぶ | 死体でも信管作動へ（二重加点は既存ガードが防ぐ） |
| 6 | 敵3機の当たり判定が描画より小さい | 設計則（スパン×スケールの直線）から外れたコピペ痕。Su-35は翼がほぼ判定球の外 | 直線値へ復帰（MiG-29/Su-33/Su-35） |

## 3. ユーザー指示の反映

- **キル時の表示を完全に無くしました**。まず「DESTROYED」の文字とキル毎の
  「TARGET n · ○○ DESTROYED」バナーを廃止し、続くご指示で残っていた
  「+1000 · Tu-95」のスコアポップも撤去（ポップアップ機構ごと削除して死にコードを残さず）。
  撃墜の手応えは爆発・無線・HUDのスコア計器が担います。
  ※ミッション進行側の「ALL TARGETS DESTROYED」等は文脈が違うので温存

## 3b. 明け方に追加で直したもの（第2ラウンド精読）

コード全体をもう一度、**別の切り口8つ**（状態遷移の全ペア／境界値とNaN／モジュール跨ぎの契約／
dtとrawDtの振り分け／生成と解放の対応／敵AIの縮退ケース／セーブ互換／HUDと内部値のズレ）で
読み直しました。効いたもの:

- **勝利が消える**: MISSION ACCOMPLISHEDの2.8秒間も対空砲・CIWS・機銃が当たり、
  勝ったはずの出撃がFAILEDに化けて記録も残りませんでした（戦域判定と飛行中ミサイルは
  既に無効化されていて、新規攻撃だけ穴が空いていた）。墜落は従来通り負けます
- **敵ミサイルがすり抜ける**: 正面から突っ込むと接近速度が秒1378mになり、1フレーム23mに対し
  信管が13m。当たるコースの弾がサンプルの間をすり抜けて飛び去っていました（自機の弾は
  対策済みで、敵側だけ古い形のまま）。フレアの判定も同じ穴
- キルカム中にGブラックアウトが実際の4倍溜まる／風の流線が止まった世界を全速で流れる
- TGTボックスだけが他のHUDと違う敵を指す（爆装中は永久に空白）、SHOOT表示が撃てない時も点灯、
  出撃終了後にガンサイトのリングが画面に貼り付く、失速警告とCHECKPOINTバナーが結果画面に残る

## 4. 基盤・ツールの穴（今後の事故を防ぐもの）

- **registry_gateが配列の中身を一切見ていなかった**: 艦のサブシステム5件を全消ししても、
  ミッションのウェーブを全消ししても「no losses」で通っていた。7/26に実際に起きた
  「隣接ハンクが閉じ括弧を共有して手前を飲む」事故は**まさに配列の中で起きる**ので、
  守るべきケースがそのまま素通りしていた。長さマーカー＋要素キーパスで検知するように修正
  （単体テスト4条件PASS）
- **護衛編成テーブルがゲートの守備範囲外**だった（19テーブル目として追加）
- payload製ユニットのジオメトリが永久リーク（payload側は1行も触らず、完成モデルを歩いて
  共有キャッシュ外だけ回収する方式で根治）
- inline_payloadの前方一致誤判定・マーカー脆弱性、`--update`が消失を黙って正本化する問題

## 5. 検証

- **全40ミッション: 正しいキーで起動 40/40・pageerror 0**（米編20＋露編20）
- registry_gate: 19テーブル・losses 0
- ワールド8プリセット×2周でGPU計数（geometries/textures/programs）**完全不動**＝マップ切替リークなし
- 同型2機を並べて片方撃墜→残る同型機が生存（ジオメトリ二重解放なし）
- 途中で自分の変更が2回ページを壊したが、いずれも**自前のスモークとゲートが検知**して
  コミット前に根治（TDZ 1件、スナップショットがゲッターを起動 1件）

## 6. 判断をお願いしたい残件（触っていません）

根拠の全文は repo 内に置きました:
`docs/findings_bughunt_r1_20260803.md`（第1ラウンド14分割の生所見）と
`docs/findings_bughunt_r2_20260803.md`（第2ラウンド8レンズ）。各項目に発火条件と最小修正案が
入っているので、やると決めたものはそのまま着手できます。

1. **飛行感に関わるバランス値**
   - F-2A / Su-47 は「ブレーキだけでは失速しない」という設計上の不変条件を破っている
   - Su-25の失速進入速度が深失速速度を下回り、低速比の正規化が壊れている
   - STABILITY正規化レンジが実データを覆えず、上下端6機（F-22とTyphoon等）が同一挙動に潰れる
   → いずれも**直すと飛び味が変わる**ので、実プレイの感触を持っているユーザー判断で
2. **`ctx.tables`がadd-only契約を迂回できる**（payloadが`MISSIONS.splice`で削除可）。
   既存3ペイロードが実際に差し替え運用しているため、凍結は`ctx.replaceMission`新設とセットでないと
   動いている納品物を壊す＝設計判断
3. **トレーサーが毎発ジオメトリを生成**（「毎フレーム生成ゼロ」のプール契約から唯一外れている）。
   艦隊戦で毎秒数十回のVBO確保/破棄。プール化は中規模の書き換えになるので夜間の単独判断を避けた
4. m-swarmの敵母機が西側機（C-17）で陣営規約から外れている＝コンテンツ判断
5. **デブリーフのTOTALと実際に記録される点数が食い違う**（難易度倍率が記録側にだけ掛かる。
   HARDでクリアすると画面は42000、ベストスコアには50400が載る）。どちらを正とするかは
   仕様判断なので触っていません
6. **レーダーが1400M圏外の接触もリムに貼り付けて描く**（6km先の目標がリング上の敵と同じ位置に
   見える）。内部プローブは「スコープ外」と判定していて食い違うが、遠方目標の方位を出すのが
   意図なら現状が正しいので保留
7. **ストーリー無線の優先度定数がペイロード側で誤り**（`CRITICAL=2`と自前定義しているが正本は3）。
   アウトロで署名台詞が黙って捨てられる。ペイロードはモデルバッチ班の所有物なので報告のみ
8. 機銃の対艦命中判定に航空機用の近距離許容が掛かり、空母だと実効半径350m（明らかに外れた
   バーストが当たる）。バランス隣接のため保留

## 7. 起きたら試せる状態になっています

いつものプレイURL（**localhost:8340**）に今夜の修正を同期済みです。**Ctrl+F5**を一度だけお願いします
（HTTP経由で確認済み: 掃引修正あり・新ゲートあり・DESTROYED表記0件）。

見てほしいポイント:
- キル時の表示が「+1000 · Tu-95」だけになっていること（ダサい英語が消えたか）
- **ロシアキャンペーンが出撃できること**（今まで1本も飛べていませんでした）
- 戦域から出たときの帰投矢印が、正しく戦域の方を指すこと
- 夜間基地（NIGHT RAID）の建物が地面に埋まらず立っていること

## 8. 触っていない領域

並走していたモデルバッチ3班（新規ユニットのモデル納品）の所有物——`payloads/**`、
モデル登録テーブル、`tools/registry_snapshot.json`——には一切書いていない。
index.htmlは共有だが、書き込み前に毎回30秒の静止確認をして自分のhunkだけコミットした。
